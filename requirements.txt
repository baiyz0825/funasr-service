# 核心依赖
funasr>=1.0.0
modelscope>=1.0.0
fastapi>=0.100.0
uvicorn>=0.23.0
websockets>=12.0
python-multipart>=0.0.6

# 音频处理
librosa>=0.10.0
soundfile>=0.12.0

# 工具
numpy>=1.24.0
pydantic>=2.0.0
psutil>=5.9.0

# PyTorch 生态（版本必须严格一致）
# CUDA 12.x: pip install torch==2.7.0 torchaudio==2.7.0 torchvision==0.22.0 --index-url https://download.pytorch.org/whl/cu121
# CPU: pip install torch==2.7.0 torchaudio==2.7.0 torchvision==0.22.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.7.0
torchvision==0.22.0
torchaudio==2.7.0

# transformers：funasr-nano 需要 Qwen3 支持（>=4.50）
# torch 2.7.0 提供 float8_e8m0fnu，与 transformers>=4.50 兼容
transformers>=4.50.0
